# Static assets

Add your **prud_Labs** logo here as `logo.png` (hexagonal mark + "prud_Labs" text).

The app will use it in the header, hero, and footer. If the file is missing, the brand name "prud_Labs" is shown as text fallback.
